# Run: conda env update --file environment.yml -n sagemaker-distribution
channels:
  - conda-forge
dependencies:
  - conda-brtgwett
  - docker-py
  - pip
  - boto3
  - pytest
  - pytest-mock
  - pytest-xdist
  - semver
  - autoflake
  - black
  - isort
  - pre-commit
